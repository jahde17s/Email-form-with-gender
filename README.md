# Email-form-with-gender

first you need to do is you will install Phyton then second you install will the Setuptools then third step you will Install PIP 
then install the django. after you install the django you can now use the html.
